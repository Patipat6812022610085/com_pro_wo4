#include <stdio.h>

int main() {
    int a = 10;
    printf("a = %d", a);
    return 0; 
}

//1. เป็น in/output ใช้เมื่อมี printf
//2. เป็น ประกาศฟังชันหลักของโปรแกรม
//3. เป็น ประกาศตัวแปรชนิด integer ชื่อ a ค่าเริ่มต้น 10
//4. เป็น แสดงค่าตัวแปร a โดยใช้รูปแบบ %d แสดงค่าจำนวนเต็ม
//5. เป็น ระบุว่าทำงานสำเร็จ