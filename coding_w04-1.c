#include <stdio.h>
int main() {
    printf("Hello,\nC Tokens!");
    return 0;
}

//1. เป็น input/output
//2. int เป็น keyword, main เป็น indentifier
//3. printf เป็น identifier, ข้อความ เป็น constant, \n เป็น operator
//4. return เป็น keyword, 0 เป็น constant